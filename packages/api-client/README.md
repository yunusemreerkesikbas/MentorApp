# @mentor/api-client

**Type-safe API client** — generated from the NestJS `/v1` OpenAPI spec via **orval** (§8).
Shared by web + (Phase 2) mobile + panel → no handwritten fetch, types don't drift.

**Status:** skeleton (base-path contract). The `generate` script activates once the api OpenAPI spec is ready.
Canonical guide: [`../../AGENTS.md`](../../AGENTS.md).
