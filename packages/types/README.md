# @mentor/types

**Stable TypeScript contracts** and enums shared by all surfaces (api/web/admin/mobile/panel):
roles, ExamType, Currency/LedgerStatus, SubscriptionTier, ApiError, Paginated (§11).

**Rule:** only stable, shared types. Feature-specific types stay in their own module.
Canonical guide: [`../../AGENTS.md`](../../AGENTS.md).
