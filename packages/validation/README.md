# @mentor/validation

**Zod schemas — the single validation source for FE + BE** (§8). Frontend forms and backend DTOs
share the same schema → single source of truth.

For now, common schemas (pagination). Feature schemas are added with their module.
Canonical guide: [`../../AGENTS.md`](../../AGENTS.md).
