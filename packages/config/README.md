# @mentor/config

Shared **tsconfig + eslint** bases. Single source of truth → apps/packages extend it.

- `tsconfig/{base,nestjs,nextjs,react-library}.json`
- `eslint/base.mjs`

Canonical guide: [`../../AGENTS.md`](../../AGENTS.md).
