# Study session ambient audio

| File | Description |
|---|---|
| `focus-ambient-soft.wav` | Soft pad loop (30s) |
| `focus-ambient-rain.wav` | Rain-ish noise loop (30s) |
| `focus-ambient-warm.wav` | Warm low pad loop (30s) |

## Regenerate

```bash
node scripts/generate-ambient-audio.mjs
```

No third-party music license — synthesized in-repo. Track catalog:
`apps/web/src/lib/ambient-tracks.ts`.
