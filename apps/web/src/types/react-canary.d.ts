// `<ViewTransition>` and `addTransitionType` ship in React's canary channel, which Next's App
// Router uses; their types live beside the stable ones in @types/react.
/// <reference types="react/canary" />
