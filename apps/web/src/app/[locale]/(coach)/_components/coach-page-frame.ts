/**
 * The frame the coach shell used to wrap around every page. Pages redesigned in the panel language
 * own their frame (`PANEL_MAIN_CLASS`); the others keep this one so moving it out of the shell
 * changed nothing for them.
 */
export const COACH_PAGE_FRAME = "mx-auto w-full max-w-6xl px-5 py-6 sm:px-8 lg:py-10";
