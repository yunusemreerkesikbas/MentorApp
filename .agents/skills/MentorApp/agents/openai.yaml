interface:
  display_name: "MentorApp"
  short_description: "Repo-specific patterns and workflows for MentorApp"
  default_prompt: "Use the MentorApp repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true